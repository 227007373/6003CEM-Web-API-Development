import Login from '@/components/login';
import Layout from '@/components/layout';
const LoginPage = () => {
    return (
        <Layout>
            <Login />
        </Layout>
    );
};
export default LoginPage;
